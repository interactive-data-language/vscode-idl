/**
 * How long we pause to make sure that tests pass after opening up windows etc
 *
 * Without this pause, if we immediately check editor windows, we have occasional failures
 */
export const DEBUG_PAUSE = 500;
