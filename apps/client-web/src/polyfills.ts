/**
 * Libs that we need at runtime
 *
 * IDK why, but webpack isn't doing anything. Might be something with NX.
 *
 * Manually importing the replacements here seems to solve the runtime problem
 */

// import 'os-browserify';
import 'path-browserify';
// import 'process';
// import 'stream-browserify';
// import 'tty';
// import 'util';
