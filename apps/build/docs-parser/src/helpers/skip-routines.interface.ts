/**
 * Routines that we skip from documenting because there or problems or they
 * should remain undocumented
 */
export const SKIP_GLOBAL: { [key: string]: boolean } = {};
SKIP_GLOBAL['crosshair'] = true;
