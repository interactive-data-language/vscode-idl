# IDL to MCP

Converts an IDL parameter that has been parsed to an MCP parameter

Built around our IDL Data Types from parsing code, documentation, and tasks.

Is a way to represent almost anything via MCP.
