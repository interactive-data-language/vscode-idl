# Assembling: Migrators types

Standalone lib with types for our Migrators so that we can correctly import the types and use them in other locations without everything else actually used by the migrators.
