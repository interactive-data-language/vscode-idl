import { ExtensionContext } from 'vscode';

/**
 * Initializes walkthrough commands for the Getting Started experience
 */
export function InitializeWalkthroughs(ctx: ExtensionContext) {
  // placeholder
}
