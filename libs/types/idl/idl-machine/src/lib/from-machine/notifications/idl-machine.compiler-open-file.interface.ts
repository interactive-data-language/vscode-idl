/**
 * Notification that we are about to compile a file
 */
export type CompilerOpenFileNotification = 'compilerOpenFile';

/** The file we are about to compile */
export type CompilerOpenFileParams = string;
