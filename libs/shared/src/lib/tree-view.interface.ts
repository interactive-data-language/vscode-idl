import { IDL_LANGUAGE_NAME } from './language.interface';

/**
 * ID of the tree view with buttons to click instead of commands to execute
 */
export const IDL_TREE_VIEW_ID = `${IDL_LANGUAGE_NAME}-tree`;
