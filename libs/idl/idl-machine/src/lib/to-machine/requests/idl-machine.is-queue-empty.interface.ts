/**
 * Is the command queue empty or not?
 */
export type IsQueueEmptyRequest = 'isQueueEmpty';

/** Parameters fo when we check if the queue is empty */
export type IsQueueEmptyParams = void;

/**
 * If your queue is empty or noy
 */
export type IsQueueEmptyResponse = boolean;
