/** Allowed values for direction */
export const DIRECTION_DOCS = {
  in: true,
  out: false,
  bidirectional: true,
  bi: true,
};

/** Allowed values for the required field */
export const REQUIRED_DOCS = { required: true, optional: true };

/** Allowed  values for the private field */
export const PRIVATE_DOCS = { private: true, public: true };
