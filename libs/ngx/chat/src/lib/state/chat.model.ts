/**
 * Re-export shared chat types from @idl/types/chat
 *
 * Types have been moved to shared library for use across frontend and backend.
 */
export type {
  ChatMessage,
  ChatMessageContent,
  ChatSession,
  ChatStateModel,
} from '@idl/types/chat';
